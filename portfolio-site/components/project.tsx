const Project = () => {
    const title: string = "Drug Info App";
    const desc: string = "Flask App with user Authentication";
    return (
        <div>
            <div className="font-bold underline">{title}</div>
            <div className="font-light">{desc}</div>
        </div>
    );

}

export default Project;